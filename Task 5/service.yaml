apiVersion: v1
kind: Service
metadata:
  name: my-app-service
spec:
  type: NodePort
  selector:
    app: my-app # <-- Service looks for this label
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80